<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Widget.Shrine.Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">?attr/colorAccent</item>
        <item name="android:theme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
    </style>

    <style name="EstablishmentRating" parent="@android:style/Widget.RatingBar">
        <!--<item name="android:minHeight">32dip</item>-->
        <!--<item name="android:minWidth">36dip</item>-->
        <!--<item name="android:minHeight">12sp</item>-->
        <!--<item name="android:minWidth">16sp</item>-->
        <item name="android:progressDrawable">@drawable/rating_bar_icon</item>
    </style>

</resources>
